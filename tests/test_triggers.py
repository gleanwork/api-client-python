"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from glean.api_client import Glean, models
import os
from tests.test_client import create_test_http_client


def test_triggers_platform_triggers_create():
    test_http_client = create_test_http_client("platform-triggers-create")

    with Glean(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_token=os.getenv("GLEAN_API_TOKEN", "value"),
    ) as glean:
        assert glean is not None

        res = glean.triggers.create(
            preset_id="GITHUB_1",
            delivery={
                "webhook_url": "https://customer.app/webhook",
                "auth": {
                    "type": models.PlatformTriggerAuthType.BEARER,
                    "secret": "secret_test_123",
                },
            },
            description="Reviews I am tagged on, sent to my team's review channel",
            inputs={
                "repository": "acme/payments-api",
            },
        )
        assert res is not None


def test_triggers_platform_triggers_list():
    test_http_client = create_test_http_client("platform-triggers-list")

    with Glean(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_token=os.getenv("GLEAN_API_TOKEN", "value"),
    ) as glean:
        assert glean is not None

        res = glean.triggers.list(page_size=50)
        assert res is not None


def test_triggers_platform_triggers_get():
    test_http_client = create_test_http_client("platform-triggers-get")

    with Glean(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_token=os.getenv("GLEAN_API_TOKEN", "value"),
    ) as glean:
        assert glean is not None

        res = glean.triggers.get(trigger_id="<id>")
        assert res is not None


def test_triggers_platform_triggers_update():
    test_http_client = create_test_http_client("platform-triggers-update")

    with Glean(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_token=os.getenv("GLEAN_API_TOKEN", "value"),
    ) as glean:
        assert glean is not None

        res = glean.triggers.update(
            trigger_id="<id>",
            status=models.PlatformTriggerStatus.ENABLED,
            description="Reviews I am tagged on, sent to my team's review channel",
            inputs={
                "repository": "acme/payments-api",
            },
            delivery={
                "webhook_url": "https://customer.app/webhook",
                "auth": {
                    "type": models.PlatformTriggerAuthType.BEARER,
                    "secret": "secret_test_123",
                },
            },
        )
        assert res is not None


def test_triggers_platform_triggers_events_search():
    test_http_client = create_test_http_client("platform-triggers-events-search")

    with Glean(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_token=os.getenv("GLEAN_API_TOKEN", "value"),
    ) as glean:
        assert glean is not None

        res = glean.triggers.search_events(trigger_id="<id>", page_size=10)
        assert res is not None


def test_triggers_platform_trigger_presets_list():
    test_http_client = create_test_http_client("platform-trigger-presets-list")

    with Glean(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_token=os.getenv("GLEAN_API_TOKEN", "value"),
    ) as glean:
        assert glean is not None

        res = glean.triggers.list_presets(page_size=50)
        assert res is not None


def test_triggers_platform_trigger_presets_get():
    test_http_client = create_test_http_client("platform-trigger-presets-get")

    with Glean(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_token=os.getenv("GLEAN_API_TOKEN", "value"),
    ) as glean:
        assert glean is not None

        res = glean.triggers.get_preset(preset_id="<id>")
        assert res is not None


def test_triggers_platform_trigger_presets_input_values_list():
    test_http_client = create_test_http_client(
        "platform-trigger-presets-input-values-list"
    )

    with Glean(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_token=os.getenv("GLEAN_API_TOKEN", "value"),
    ) as glean:
        assert glean is not None

        res = glean.triggers.list_preset_input_values(preset_id="<id>", field="<value>")
        assert res is not None


def test_triggers_platform_trigger_presets_events_search():
    test_http_client = create_test_http_client("platform-trigger-presets-events-search")

    with Glean(
        server_url=os.getenv("TEST_SERVER_URL", "http://localhost:18080"),
        client=test_http_client,
        api_token=os.getenv("GLEAN_API_TOKEN", "value"),
    ) as glean:
        assert glean is not None

        res = glean.triggers.search_preset_events(
            preset_id="<id>",
            inputs={
                "repository": "acme/payments-api",
            },
            page_size=10,
        )
        assert res is not None
