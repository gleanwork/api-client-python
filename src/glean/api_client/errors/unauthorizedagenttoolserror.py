"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from dataclasses import dataclass, field
from glean.api_client.errors import GleanBaseError
from glean.api_client.models import servertoolrequest as models_servertoolrequest
from glean.api_client.types import BaseModel
import httpx
import pydantic
from typing import List, Optional
from typing_extensions import Annotated


class UnauthorizedAgentToolsErrorData(BaseModel):
    message: str
    r"""Human-readable description of the failure."""
    authentication_suggestions: Annotated[
        List[models_servertoolrequest.ServerToolRequest],
        pydantic.Field(alias="authenticationSuggestions"),
    ]
    r"""One entry per tool the caller must authorize."""


@dataclass(unsafe_hash=True)
class UnauthorizedAgentToolsError(GleanBaseError):
    r"""Returned when the agent has tools the caller must authorize before the run can start. Each entry in `authenticationSuggestions` names one such tool; POST its `serverId` to `/tool-servers/{serverId}/auth` with `returnUrl` in the request body to obtain an `authorizationUrl` to redirect the end user to, then retry the run once OAuth completes."""

    data: UnauthorizedAgentToolsErrorData = field(hash=False)

    def __init__(
        self,
        data: UnauthorizedAgentToolsErrorData,
        raw_response: httpx.Response,
        body: Optional[str] = None,
    ):
        fallback = body or raw_response.text
        message = str(data.message) or fallback
        super().__init__(message, raw_response, body)
        object.__setattr__(self, "data", data)
