"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from glean.api_client.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class ActionSummaryTypedDict(TypedDict):
    r"""Represents a minimal summary of an action."""

    tool_id: str
    r"""The unique identifier of the action."""
    display_name: str
    r"""The display name of the action."""
    type: NotRequired[str]
    r"""The type of tool - RETRIEVAL for read-only operations, ACTION for operations that modify data."""
    auth_type: NotRequired[str]
    r"""The authentication type required - OAUTH_USER, OAUTH_ADMIN, API_KEY, BASIC_AUTH, DWD (domain-wide delegation), or NONE."""
    write_action_type: NotRequired[str]
    r"""For write actions only - REDIRECT (client renders URL) or EXECUTION (external server call)."""
    is_setup_finished: NotRequired[bool]
    r"""Whether this action has been fully configured and validated."""
    data_source: NotRequired[str]
    r"""Indicates the kind of knowledge a tool would access or modify.
    Company knowledge:
    - Glean search, and any native tools that derive from it (e.g., expert search, code search)
    - Native federated tools to company data sources (e.g., outlook search)
    World knowledge:
    - Platform action like bravewebsearch, geminiwebsearch, etc
    Neutral knowledge:
    - Native tools that don't access or modify content via APIs (e.g., file analyst, think)
    - Platform read or write tools (creator has to determine their knowledge implications)

    """


class ActionSummary(BaseModel):
    r"""Represents a minimal summary of an action."""

    tool_id: str
    r"""The unique identifier of the action."""

    display_name: str
    r"""The display name of the action."""

    type: Optional[str] = None
    r"""The type of tool - RETRIEVAL for read-only operations, ACTION for operations that modify data."""

    auth_type: Optional[str] = None
    r"""The authentication type required - OAUTH_USER, OAUTH_ADMIN, API_KEY, BASIC_AUTH, DWD (domain-wide delegation), or NONE."""

    write_action_type: Optional[str] = None
    r"""For write actions only - REDIRECT (client renders URL) or EXECUTION (external server call)."""

    is_setup_finished: Optional[bool] = None
    r"""Whether this action has been fully configured and validated."""

    data_source: Optional[str] = None
    r"""Indicates the kind of knowledge a tool would access or modify.
    Company knowledge:
    - Glean search, and any native tools that derive from it (e.g., expert search, code search)
    - Native federated tools to company data sources (e.g., outlook search)
    World knowledge:
    - Platform action like bravewebsearch, geminiwebsearch, etc
    Neutral knowledge:
    - Native tools that don't access or modify content via APIs (e.g., file analyst, think)
    - Platform read or write tools (creator has to determine their knowledge implications)

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "type",
                "auth_type",
                "write_action_type",
                "is_setup_finished",
                "data_source",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
