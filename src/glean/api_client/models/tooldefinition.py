"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .toolannotations import ToolAnnotations, ToolAnnotationsTypedDict
from glean.api_client.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import Any, Dict, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class ToolDefinitionTypedDict(TypedDict):
    server_id: str
    r"""Tool server this tool belongs to."""
    name: str
    r"""Name to invoke the tool by."""
    display_name: NotRequired[str]
    r"""Human-readable name of the tool."""
    description: NotRequired[str]
    r"""What the tool does, as surfaced to a model."""
    input_schema: NotRequired[Dict[str, Any]]
    r"""JSON Schema for the tool's input parameters."""
    annotations: NotRequired[ToolAnnotationsTypedDict]
    r"""Behavioural hints for the tool, exactly as declared by the tool server. No
    defaults are filled in — see the per-field notes for what an absent value means.

    Per the MCP specification, clients MUST treat these hints as untrusted unless
    the tool server is itself trusted. They are advisory metadata, not an
    authorization signal, and must not be the sole basis of a security decision.

    """


class ToolDefinition(BaseModel):
    server_id: Annotated[str, pydantic.Field(alias="serverId")]
    r"""Tool server this tool belongs to."""

    name: str
    r"""Name to invoke the tool by."""

    display_name: Annotated[Optional[str], pydantic.Field(alias="displayName")] = None
    r"""Human-readable name of the tool."""

    description: Optional[str] = None
    r"""What the tool does, as surfaced to a model."""

    input_schema: Annotated[
        Optional[Dict[str, Any]], pydantic.Field(alias="inputSchema")
    ] = None
    r"""JSON Schema for the tool's input parameters."""

    annotations: Optional[ToolAnnotations] = None
    r"""Behavioural hints for the tool, exactly as declared by the tool server. No
    defaults are filled in — see the per-field notes for what an absent value means.

    Per the MCP specification, clients MUST treat these hints as untrusted unless
    the tool server is itself trusted. They are advisory metadata, not an
    authorization signal, and must not be the sole basis of a security decision.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["displayName", "description", "inputSchema", "annotations"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    ToolDefinition.model_rebuild()
except NameError:
    pass
