"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from glean.api_client.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
import pydantic
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class ToolAnnotationsTypedDict(TypedDict):
    r"""Behavioural hints for the tool, exactly as declared by the tool server. No
    defaults are filled in — see the per-field notes for what an absent value means.

    Per the MCP specification, clients MUST treat these hints as untrusted unless
    the tool server is itself trusted. They are advisory metadata, not an
    authorization signal, and must not be the sole basis of a security decision.

    """

    title: NotRequired[str]
    read_only_hint: NotRequired[bool]
    r"""True if the tool does not modify its environment. Always present. MCP
    defines no distinction between an undeclared value and `false` here, so
    `false` means \"not declared read-only\" rather than \"declared mutating\".

    """
    destructive_hint: NotRequired[Nullable[bool]]
    r"""True if the tool may perform destructive updates. **Absent means the server
    did not declare it**, for which the MCP default is `true`. Only meaningful
    when `readOnlyHint` is `false`.

    """
    idempotent_hint: NotRequired[bool]
    r"""True if repeated calls with the same arguments have no additional effect.
    Always present; as with `readOnlyHint`, `false` means \"not declared
    idempotent\". Only meaningful when `readOnlyHint` is `false`.

    """
    open_world_hint: NotRequired[Nullable[bool]]
    r"""True if the tool interacts with an open world of external entities. **Absent
    means the server did not declare it**, for which the MCP default is `true`.

    """


class ToolAnnotations(BaseModel):
    r"""Behavioural hints for the tool, exactly as declared by the tool server. No
    defaults are filled in — see the per-field notes for what an absent value means.

    Per the MCP specification, clients MUST treat these hints as untrusted unless
    the tool server is itself trusted. They are advisory metadata, not an
    authorization signal, and must not be the sole basis of a security decision.

    """

    title: Optional[str] = None

    read_only_hint: Annotated[Optional[bool], pydantic.Field(alias="readOnlyHint")] = (
        None
    )
    r"""True if the tool does not modify its environment. Always present. MCP
    defines no distinction between an undeclared value and `false` here, so
    `false` means \"not declared read-only\" rather than \"declared mutating\".

    """

    destructive_hint: Annotated[
        OptionalNullable[bool], pydantic.Field(alias="destructiveHint")
    ] = UNSET
    r"""True if the tool may perform destructive updates. **Absent means the server
    did not declare it**, for which the MCP default is `true`. Only meaningful
    when `readOnlyHint` is `false`.

    """

    idempotent_hint: Annotated[
        Optional[bool], pydantic.Field(alias="idempotentHint")
    ] = None
    r"""True if repeated calls with the same arguments have no additional effect.
    Always present; as with `readOnlyHint`, `false` means \"not declared
    idempotent\". Only meaningful when `readOnlyHint` is `false`.

    """

    open_world_hint: Annotated[
        OptionalNullable[bool], pydantic.Field(alias="openWorldHint")
    ] = UNSET
    r"""True if the tool interacts with an open world of external entities. **Absent
    means the server did not declare it**, for which the MCP default is `true`.

    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "title",
                "readOnlyHint",
                "destructiveHint",
                "idempotentHint",
                "openWorldHint",
            ]
        )
        nullable_fields = set(["destructiveHint", "openWorldHint"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    ToolAnnotations.model_rebuild()
except NameError:
    pass
