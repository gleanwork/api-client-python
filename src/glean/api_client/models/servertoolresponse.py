"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .authcontext import AuthContext, AuthContextTypedDict
from enum import Enum
from glean.api_client import models, utils
from glean.api_client.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class ServerToolResponseRequestType(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""The type of request made to the user."""

    # Request for approving execution of the tool.
    EXECUTION = "EXECUTION"
    # Request for authenticating to a tool, provided as a (tool) suggestion.
    AUTHENTICATION_SUGGESTION = "AUTHENTICATION_SUGGESTION"
    # Suggestion to vote for enabling an available-but-not-enabled tool.
    VOTE_SUGGESTION = "VOTE_SUGGESTION"
    # Request to approve an outbound/egress call from the sandbox.
    SANDBOX_EGRESS = "SANDBOX_EGRESS"


class GrantScope(str, Enum, metaclass=utils.OpenEnumMeta):
    r"""Scope of the approval grant. Only applicable when isGranted is true and requestType is EXECUTION."""

    # Approve only the current tool request. Future tool execution requests still require approval.
    CURRENT_REQUEST = "CURRENT_REQUEST"
    # Approve the current tool request and auto-approve all future same tool requests in this chat session.
    CURRENT_SESSION = "CURRENT_SESSION"
    # Approve the current tool request and auto-approve all future same tool requests across all chat sessions.
    ALWAYS = "ALWAYS"


class ServerToolResponseTypedDict(TypedDict):
    r"""Response to a server tool request. The applicable fields depend on requestType:

    For EXECUTION requests:
    - isGranted: whether tool execution is approved
    - reason: optional explanation

    For AUTHENTICATION_SUGGESTION requests:
    - isGranted: whether auth completed successfully (true=connected, false=skipped)
    - authContext: contains serverId or actionPackId for identifying the authenticated entity
    - reason: optional explanation for skip

    For VOTE_SUGGESTION requests:
    - voted: whether the user voted for this tool

    For SANDBOX_EGRESS requests:
    - isGranted: whether the sandbox egress call is approved

    """

    request_type: ServerToolResponseRequestType
    r"""The type of request made to the user."""
    request_id: str
    r"""Unique identifier for this request."""
    is_granted: NotRequired[bool]
    r"""Whether tool request is granted (indicates approval for execution, or completion for auth)."""
    grant_scope: NotRequired[GrantScope]
    r"""Scope of the approval grant. Only applicable when isGranted is true and requestType is EXECUTION.

    """
    auth_context: NotRequired[AuthContextTypedDict]
    r"""Context for authentication responses, containing identifiers for the entity being authenticated.

    """


class ServerToolResponse(BaseModel):
    r"""Response to a server tool request. The applicable fields depend on requestType:

    For EXECUTION requests:
    - isGranted: whether tool execution is approved
    - reason: optional explanation

    For AUTHENTICATION_SUGGESTION requests:
    - isGranted: whether auth completed successfully (true=connected, false=skipped)
    - authContext: contains serverId or actionPackId for identifying the authenticated entity
    - reason: optional explanation for skip

    For VOTE_SUGGESTION requests:
    - voted: whether the user voted for this tool

    For SANDBOX_EGRESS requests:
    - isGranted: whether the sandbox egress call is approved

    """

    request_type: Annotated[
        ServerToolResponseRequestType, pydantic.Field(alias="requestType")
    ]
    r"""The type of request made to the user."""

    request_id: Annotated[str, pydantic.Field(alias="requestId")]
    r"""Unique identifier for this request."""

    is_granted: Annotated[Optional[bool], pydantic.Field(alias="isGranted")] = None
    r"""Whether tool request is granted (indicates approval for execution, or completion for auth)."""

    grant_scope: Annotated[Optional[GrantScope], pydantic.Field(alias="grantScope")] = (
        GrantScope.CURRENT_REQUEST
    )
    r"""Scope of the approval grant. Only applicable when isGranted is true and requestType is EXECUTION.

    """

    auth_context: Annotated[
        Optional[AuthContext], pydantic.Field(alias="authContext")
    ] = None
    r"""Context for authentication responses, containing identifiers for the entity being authenticated.

    """

    @field_serializer("request_type")
    def serialize_request_type(self, value):
        if isinstance(value, str):
            try:
                return models.ServerToolResponseRequestType(value)
            except ValueError:
                return value
        return value

    @field_serializer("grant_scope")
    def serialize_grant_scope(self, value):
        if isinstance(value, str):
            try:
                return models.GrantScope(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["isGranted", "grantScope", "authContext"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    ServerToolResponse.model_rebuild()
except NameError:
    pass
