"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .platformauthenticationsuggestion import (
    PlatformAuthenticationSuggestion,
    PlatformAuthenticationSuggestionTypedDict,
)
from .platformproblemdetail import PlatformProblemDetail, PlatformProblemDetailTypedDict
from enum import Enum
from glean.api_client.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class PlatformSkillSourcePreviewStreamErrorType(str, Enum):
    r"""Event type, also sent as the SSE `event` name."""

    ERROR = "ERROR"


class PlatformSkillSourcePreviewStreamErrorTypedDict(TypedDict):
    r"""Terminal failure event carrying the failure as a problem detail. Most GitHub authorization failures return HTTP 422 before the stream opens. If authorization fails after the stream opens, `error.code` is `tools_unauthorized` and `authentication_suggestions` is present."""

    type: PlatformSkillSourcePreviewStreamErrorType
    r"""Event type, also sent as the SSE `event` name."""
    error: PlatformProblemDetailTypedDict
    r"""Error response following RFC 9457, extended with `code` and `documentation_url` for machine-readable classification and self-service remediation.

    """
    authentication_suggestions: NotRequired[
        List[PlatformAuthenticationSuggestionTypedDict]
    ]
    r"""Present only when `error.code` is `tools_unauthorized`. Names the GitHub action pack to authorize, with the same retry flow as the 422 response."""


class PlatformSkillSourcePreviewStreamError(BaseModel):
    r"""Terminal failure event carrying the failure as a problem detail. Most GitHub authorization failures return HTTP 422 before the stream opens. If authorization fails after the stream opens, `error.code` is `tools_unauthorized` and `authentication_suggestions` is present."""

    type: PlatformSkillSourcePreviewStreamErrorType
    r"""Event type, also sent as the SSE `event` name."""

    error: PlatformProblemDetail
    r"""Error response following RFC 9457, extended with `code` and `documentation_url` for machine-readable classification and self-service remediation.

    """

    authentication_suggestions: Optional[List[PlatformAuthenticationSuggestion]] = None
    r"""Present only when `error.code` is `tools_unauthorized`. Names the GitHub action pack to authorize, with the same retry flow as the 422 response."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["authentication_suggestions"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
