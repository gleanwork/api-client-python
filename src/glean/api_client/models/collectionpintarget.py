"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .collectionpinnablecategories import CollectionPinnableCategories
from .collectionpinnabletargets import CollectionPinnableTargets
from glean.api_client import models
from glean.api_client.types import BaseModel, UNSET_SENTINEL
from pydantic import field_serializer, model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class CollectionPinTargetTypedDict(TypedDict):
    category: CollectionPinnableCategories
    r"""Categories a Collection can be pinned to."""
    value: NotRequired[str]
    r"""Optional. If category supports values, then the additional value for the category e.g. department name for DEPARTMENT_RESOURCE, team name/id for TEAM_RESOURCE and so on."""
    target: NotRequired[CollectionPinnableTargets]
    r"""What targets can a Collection be pinned to."""


class CollectionPinTarget(BaseModel):
    category: CollectionPinnableCategories
    r"""Categories a Collection can be pinned to."""

    value: Optional[str] = None
    r"""Optional. If category supports values, then the additional value for the category e.g. department name for DEPARTMENT_RESOURCE, team name/id for TEAM_RESOURCE and so on."""

    target: Optional[CollectionPinnableTargets] = None
    r"""What targets can a Collection be pinned to."""

    @field_serializer("category")
    def serialize_category(self, value):
        if isinstance(value, str):
            try:
                return models.CollectionPinnableCategories(value)
            except ValueError:
                return value
        return value

    @field_serializer("target")
    def serialize_target(self, value):
        if isinstance(value, str):
            try:
                return models.CollectionPinnableTargets(value)
            except ValueError:
                return value
        return value

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["value", "target"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
