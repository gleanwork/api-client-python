"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from glean.api_client import errors, models, utils
from glean.api_client._hooks import HookContext
from glean.api_client.types import OptionalNullable, UNSET
from glean.api_client.utils import eventstreaming, get_security_from_env
from glean.api_client.utils.unmarshal_json_response import unmarshal_json_response
from typing import Any, Mapping, Optional, Union


class ChatSDK(BaseSDK):
    def create(
        self,
        *,
        input: Union[
            models.PlatformChatCreateInput, models.PlatformChatCreateInputTypedDict
        ],
        store: Optional[bool] = True,
        conversation_id: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PlatformChatCompletedResponse:
        r"""Create a chat response

        Run an assistant turn. The default response is JSON. HTTP clients request server-sent events by setting `stream` to true in the JSON body. An `Accept: text/event-stream` header does not replace `stream`.


        :param input: Either a plain string (single user turn) or a chronological array of `USER`/`ASSISTANT` messages. The final array message must be `USER`.

        :param store: When true (default), persist the interaction and return a `conversation_id`. When false, run ephemerally with no persistence.

        :param conversation_id: Continue an existing stored conversation. Incompatible with message-array `input` and with `store: false`.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.PlatformChatCreateRequest(
            input=utils.get_pydantic_model(input, models.PlatformChatCreateInput),
            store=store,
            conversation_id=conversation_id,
        )

        req = self._build_request(
            method="POST",
            path="/api/chat",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.PlatformChatCreateRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="platform-chat-create",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Chat"],
                extensions={
                    "x-codegen-request-body-name": "payload",
                    "x-glean-experimental": {
                        "id": "e65a0a77-97a2-4c91-be60-eae7f6238015",
                        "introduced": "2026-07-14",
                    },
                    "x-visibility": "Public",
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.PlatformChatCompletedResponse, http_res
            )
        if utils.match_response(
            http_res,
            ["400", "401", "403", "404", "408", "413", "422", "429"],
            "application/problem+json",
        ):
            response_data = unmarshal_json_response(
                errors.PlatformProblemDetailErrorData, http_res
            )
            raise errors.PlatformProblemDetailError(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.PlatformProblemDetailErrorData, http_res
            )
            raise errors.PlatformProblemDetailError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.GleanError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.GleanError("API error occurred", http_res, http_res_text)

        raise errors.GleanError("Unexpected response received", http_res)

    async def create_async(
        self,
        *,
        input: Union[
            models.PlatformChatCreateInput, models.PlatformChatCreateInputTypedDict
        ],
        store: Optional[bool] = True,
        conversation_id: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.PlatformChatCompletedResponse:
        r"""Create a chat response

        Run an assistant turn. The default response is JSON. HTTP clients request server-sent events by setting `stream` to true in the JSON body. An `Accept: text/event-stream` header does not replace `stream`.


        :param input: Either a plain string (single user turn) or a chronological array of `USER`/`ASSISTANT` messages. The final array message must be `USER`.

        :param store: When true (default), persist the interaction and return a `conversation_id`. When false, run ephemerally with no persistence.

        :param conversation_id: Continue an existing stored conversation. Incompatible with message-array `input` and with `store: false`.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.PlatformChatCreateRequest(
            input=utils.get_pydantic_model(input, models.PlatformChatCreateInput),
            store=store,
            conversation_id=conversation_id,
        )

        req = self._build_request_async(
            method="POST",
            path="/api/chat",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.PlatformChatCreateRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="platform-chat-create",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Chat"],
                extensions={
                    "x-codegen-request-body-name": "payload",
                    "x-glean-experimental": {
                        "id": "e65a0a77-97a2-4c91-be60-eae7f6238015",
                        "introduced": "2026-07-14",
                    },
                    "x-visibility": "Public",
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.PlatformChatCompletedResponse, http_res
            )
        if utils.match_response(
            http_res,
            ["400", "401", "403", "404", "408", "413", "422", "429"],
            "application/problem+json",
        ):
            response_data = unmarshal_json_response(
                errors.PlatformProblemDetailErrorData, http_res
            )
            raise errors.PlatformProblemDetailError(response_data, http_res)
        if utils.match_response(http_res, ["500", "503"], "application/problem+json"):
            response_data = unmarshal_json_response(
                errors.PlatformProblemDetailErrorData, http_res
            )
            raise errors.PlatformProblemDetailError(response_data, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.GleanError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.GleanError("API error occurred", http_res, http_res_text)

        raise errors.GleanError("Unexpected response received", http_res)

    def create_stream(
        self,
        *,
        input: Union[
            models.PlatformChatCreateStreamInput,
            models.PlatformChatCreateStreamInputTypedDict,
        ],
        store: Optional[bool] = True,
        conversation_id: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> eventstreaming.EventStream[models.PlatformChatStreamEventServerSentEvent]:
        r"""SDK-only logical operation. HTTP clients must call the base path; the URL fragment is not sent. Create a chat response

        SDK-only logical operation. HTTP clients must call the base path; the URL fragment is not sent. Run an assistant turn. The default response is JSON. HTTP clients request server-sent events by setting `stream` to true in the JSON body. An `Accept: text/event-stream` header does not replace `stream`.


        :param input: Either a plain string (single user turn) or a chronological array of `USER`/`ASSISTANT` messages. The final array message must be `USER`.

        :param store: When true (default), persist the interaction and return a `conversation_id`. When false, run ephemerally with no persistence.

        :param conversation_id: Continue an existing stored conversation. Incompatible with message-array `input` and with `store: false`.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.PlatformChatCreateStreamRequest(
            input=utils.get_pydantic_model(input, models.PlatformChatCreateStreamInput),
            store=store,
            conversation_id=conversation_id,
        )

        req = self._build_request(
            method="POST",
            path="/api/chat#stream",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="text/event-stream",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.PlatformChatCreateStreamRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="platform-chat-create-stream",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Chat"],
                extensions={
                    "x-codegen-request-body-name": "payload",
                    "x-glean-experimental": {
                        "id": "e65a0a77-97a2-4c91-be60-eae7f6238015",
                        "introduced": "2026-07-14",
                    },
                    "x-visibility": "Public",
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            stream=True,
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "text/event-stream"):
            return eventstreaming.EventStream(
                http_res,
                lambda raw: unmarshal_json_response(
                    models.PlatformChatStreamEventServerSentEvent, http_res, raw
                ),
                client_ref=self,
            )
        if utils.match_response(
            http_res,
            ["400", "401", "403", "404", "408", "413", "422", "429"],
            "application/problem+json",
        ):
            http_res_text = utils.stream_to_text(http_res)
            response_data = unmarshal_json_response(
                errors.PlatformProblemDetailErrorData, http_res, http_res_text
            )
            raise errors.PlatformProblemDetailError(
                response_data, http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "503"], "application/problem+json"):
            http_res_text = utils.stream_to_text(http_res)
            response_data = unmarshal_json_response(
                errors.PlatformProblemDetailErrorData, http_res, http_res_text
            )
            raise errors.PlatformProblemDetailError(
                response_data, http_res, http_res_text
            )
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.GleanError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise errors.GleanError("API error occurred", http_res, http_res_text)

        http_res_text = utils.stream_to_text(http_res)
        raise errors.GleanError("Unexpected response received", http_res, http_res_text)

    async def create_stream_async(
        self,
        *,
        input: Union[
            models.PlatformChatCreateStreamInput,
            models.PlatformChatCreateStreamInputTypedDict,
        ],
        store: Optional[bool] = True,
        conversation_id: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> eventstreaming.EventStreamAsync[models.PlatformChatStreamEventServerSentEvent]:
        r"""SDK-only logical operation. HTTP clients must call the base path; the URL fragment is not sent. Create a chat response

        SDK-only logical operation. HTTP clients must call the base path; the URL fragment is not sent. Run an assistant turn. The default response is JSON. HTTP clients request server-sent events by setting `stream` to true in the JSON body. An `Accept: text/event-stream` header does not replace `stream`.


        :param input: Either a plain string (single user turn) or a chronological array of `USER`/`ASSISTANT` messages. The final array message must be `USER`.

        :param store: When true (default), persist the interaction and return a `conversation_id`. When false, run ephemerally with no persistence.

        :param conversation_id: Continue an existing stored conversation. Incompatible with message-array `input` and with `store: false`.

        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.PlatformChatCreateStreamRequest(
            input=utils.get_pydantic_model(input, models.PlatformChatCreateStreamInput),
            store=store,
            conversation_id=conversation_id,
        )

        req = self._build_request_async(
            method="POST",
            path="/api/chat#stream",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="text/event-stream",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.PlatformChatCreateStreamRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="platform-chat-create-stream",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Chat"],
                extensions={
                    "x-codegen-request-body-name": "payload",
                    "x-glean-experimental": {
                        "id": "e65a0a77-97a2-4c91-be60-eae7f6238015",
                        "introduced": "2026-07-14",
                    },
                    "x-visibility": "Public",
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            stream=True,
            retry_config=retry_config,
        )

        response_data: Any = None
        if utils.match_response(http_res, "200", "text/event-stream"):
            return eventstreaming.EventStreamAsync(
                http_res,
                lambda raw: unmarshal_json_response(
                    models.PlatformChatStreamEventServerSentEvent, http_res, raw
                ),
                client_ref=self,
            )
        if utils.match_response(
            http_res,
            ["400", "401", "403", "404", "408", "413", "422", "429"],
            "application/problem+json",
        ):
            http_res_text = await utils.stream_to_text_async(http_res)
            response_data = unmarshal_json_response(
                errors.PlatformProblemDetailErrorData, http_res, http_res_text
            )
            raise errors.PlatformProblemDetailError(
                response_data, http_res, http_res_text
            )
        if utils.match_response(http_res, ["500", "503"], "application/problem+json"):
            http_res_text = await utils.stream_to_text_async(http_res)
            response_data = unmarshal_json_response(
                errors.PlatformProblemDetailErrorData, http_res, http_res_text
            )
            raise errors.PlatformProblemDetailError(
                response_data, http_res, http_res_text
            )
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.GleanError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise errors.GleanError("API error occurred", http_res, http_res_text)

        http_res_text = await utils.stream_to_text_async(http_res)
        raise errors.GleanError("Unexpected response received", http_res, http_res_text)
